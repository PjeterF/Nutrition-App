@model List<Food>

<div class="border border-5 border-secondary rounded-3">
    <a class="btn btn-secondary" asp-controller="NutrientCalculator" asp-action="FindItem">Find food item</a>
    <br />
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            @for(int i=0;i<Model.Count;i++)
            {
                <tr>
                     <td>@Model[i].Name</td>
                </tr>
            }
        </tbody>
    </table>
</div>